# macOS
.DS_Store

# Working files — not part of the shipping site
scratch/
screenshots/
uploads/

# Build deps
node_modules/

# Generated static site (built in CI on every push)
site/

# Backend runtime data (the live database — keep out of git)
content.db
content.db-journal
content.db-wal
content.db-shm

# Vercel
.vercel
.env*
